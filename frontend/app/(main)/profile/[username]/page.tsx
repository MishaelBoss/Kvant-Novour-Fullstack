import ProfileContent from "./ProfileContent";

export const metadata = {
    title: 'Профиль',
};

export default function Page(){
    return (
        <>
        <ProfileContent/>
        </>
    ); 
}