interface Props {
    children: React.ReactNode;
    fetch: () => Promise<void>;
}


export default function CreateStudyGroupModal({ children, fetch }: Props) {
    return (
        <>
        </>
    )
};